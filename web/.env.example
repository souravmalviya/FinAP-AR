# URL of the deployed pipeline backend (no trailing slash).
# Locally you can leave this unset - it defaults to http://localhost:5000.
# On Vercel: set this to your Railway backend URL, e.g. https://finerp-ap.up.railway.app
NEXT_PUBLIC_API_URL=
